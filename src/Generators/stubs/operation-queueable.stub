<?php

namespace {{namespace}};

use {{unit_namespace}}\QueueableOperation;
use Illuminate\Http\Request;
{{use_jobs}}
class {{operation}} extends QueueableOperation
{
    public function handle(Request $request)
    {
{{run_jobs}}
    }
}
